{
  "name": "httpspy-tests",
  "private": true,
  "type": "module",
  "description": "Offline Luau checks for HttpSpy (Luau WebAssembly runtime, no Roblox client needed).",
  "scripts": {
    "check": "node run_checks.mjs"
  },
  "devDependencies": {
    "@luau-rs/luau": "0.739.0"
  }
}
